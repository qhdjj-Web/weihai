# 🎬 第一次运行检查清单

打开应用前，请确认以下条件：

## ✅ 系统要求

- [ ] 已安装现代浏览器（Chrome/Firefox/Safari/Edge）
- [ ] 网络正常连接（加载高德地图需要）
- [ ] 浏览器允许 JavaScript 执行
- [ ] 浏览器允许 localStorage 存储

## ✅ 文件完整性

- [ ] `index.html` 存在
- [ ] 文件大小 > 50KB（代码完整）
- [ ] 文件没有损坏打开

## 🚀 首次使用步骤

### 第1步：打开应用
```
双击 index.html
或
在浏览器中 Ctrl+O / Cmd+O 打开 index.html 文件
```

**预期现象**：
- 浏览器显示"威海景点规划"标题
- 左侧显示威海地图
- 右侧显示景点列表
- 可以看到7个预置景点

⏱️ 如果不出现，检查：
- 网络连接是否正常
- 浏览器是否有错误（按 F12 查看控制台）
- 是否禁用了 JavaScript

### 第2步：测试地图交互
```
1. 左侧地图应该可以拖拽和缩放
2. 地图上应该看到7个彩色标记
3. 点击标记应该显示景点信息
```

**如果不行**：
- 高德地图可能无法加载（网络问题）
- 刷新页面重试
- 检查浏览器控制台错误

### 第3步：测试景点清单
```
1. 右侧应该显示7个景点
2. 每个景点右侧有"🖼️换图"和时间信息
3. 向下滚动应该能看到所有景点
```

**如果不行**：
- 确认列表有上下滚动条
- F12 检查是否有 JavaScript 错误

### 第4步：测试勾选功能
```
1. 点击景点左侧的复选框
2. 景点应该变成蓝色背景
3. 地图上的标记应该变大
```

**如果不行**：
- 检查浏览器是否支持 ES6+JavaScript
- 刷新页面重试

### 第5步：测试生成路线
```
1. 勾选2-3个景点
2. 点击"📋 生成路线"按钮
3. 应该跳转到"路线规划"标签页
4. 看到结构化的出行方案
```

**如果不行**：
- 确保已勾选景点
- 点击可能没反应，检查浏览器控制台

### 第6步：测试复制导出
```
1. 点击"📄 复制导出"
2. 应该收到"已复制到剪贴板"的提示
3. 粘贴到记事本应该看到完整的路线
```

**如果不行**：
- 先执行"生成路线"步骤
- 可能是浏览器权限问题

### 第7步：测试数据持久化
```
1. 勾选几个景点
2. 刷新页面（Ctrl+R 或 Cmd+R）
3. 刷新后勾选状态应该保留
```

**如果不行**：
- 浏览器可能禁用了 localStorage
- 检查浏览器隐私设置

## 🔧 故障排除

### 问题：打开时完全空白
**解决**：
1. 检查网络
2. 按 F12 看错误
3. 尝试 Ctrl+Shift+Delete 清除缓存
4. 换个浏览器试试

### 问题：地图不显示
**解决**：
1. 等待10秒让高德地图加载
2. 刷新页面
3. 检查网络是否被代理/防火墙阻止
4. 如果一直不行，高德地图 API Key 可能失效

### 问题：景点列表显示但操作没反应
**解决**：
1. 按 F12 打开开发者工具
2. 切换到 Console 标签
3. 查看是否有红色错误
4. 记录错误信息，可以提交 Issue

### 问题：图片不显示
**解决**：
1. 这是正常的，预置景点使用网络图片 URL
2. 自己添加的景点图片应该显示
3. 图片 URL 加载失败时会显示灰色占位符

### 问题：添加新景点保存不了
**解决**：
1. 确保填写了所有必填项
2. 经纬度要在威海范围内（120-124, 36-38）
3. 检查浏览器控制台是否有错误
4. 检查 localStorage 是否满（很少见）

## ✨ 验收标准

当以下全部达成，说明应用工作正常：

- [x] 现在起应用正常打开
- [x] 地图显示威海景点
- [x] 可以勾选景点
- [x] 可以生成路线
- [x] 可以复制导出
- [x] 刷新后数据保留
- [x] 没有明显的错误弹窗

## 📞 需要帮助？

如果上述所有步骤都失败了：

1. **记录错误信息**
   - 打开 F12 开发者工具
   - Console 标签中的红色错误
   - 浏览器和系统信息

2. **提交 Issue**
   - 贴上错误信息
   - 描述复现步骤
   - 等待维护者回复

3. **尝试备选方案**
   - 换一个浏览器
   - 在其他电脑上尝试
   - 下载最新版浏览器

---

祝使用愉快！🎉

有任何问题，欢迎反馈。
